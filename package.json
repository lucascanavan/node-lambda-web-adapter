{
  "name": "node-lambda-web-adapter",
  "version": "1.0.0",
  "description": "Sample AWS Lambda using Function URL, nodejs, typescript, terraform & aws-lambda-web-adapter.",
  "main": "index.js",
  "scripts": {
    "dev": "ts-node-dev src",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "deploy": "./deploy.sh",
    "destroy": "cd ./terraform && terraform destroy --var-file=vars.tfvars -auto-approve"
  },
  "author": "Lucas Canavan",
  "dependencies": {
    "cookie-parser": "^1.4.6",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.8.4"
  }
}
